netstat -natpl 显示TCP的侦听端口

netstat -naupl 显示UDP的侦听端口

vim没有反应，无法输入:
	按到了Ctrl+S,导致停止向终端输出，Ctrl+Q恢复
	
安装pip3：
	sudo apt-get -y install python3-pip

centos:
	reboot
	poweroff
	
	netstat 安装：（解决没有ifconfig的问题）
		yum install net-tools
	
	查看本机DNS：
		cat /etc/resolv.conf
		vi /etc/resolv.conf 编辑 nameserver 1.1.1.1
		
	网络设置：
		ip addr 查看网卡名称，如ens33、ens32
		cd /etc/sysconfig/network-scripts 然后 ls 查看文件
		vi ifcfg-ens33 
		BOOTPROT=dhcp 动态ip
		NETMASK=255.255.255.0 
		GATEWAY=192.168.10.1 网关
		ONBOOT=yes 开机启动
		
		dhclient ens33 获取动态IP地址
		
			如果是静态IP的话：
				BOOTPROT=static
				再加上：
					IPADDR=192.168.10.XXX
		
		配置DNS服务器：
			vi /etc/resolv.conf
			nameserver 1.1.1.1
			nameserver 8.8.8.8
		进行网络相关的更改后记得 重启网络服务
		
		但是这里配置的DNS服务器会被覆盖
		
		Centos DNS服务器被覆盖解决办法： 
			https://blog.csdn.net/typa01_kk/article/details/49155949		
			vi /etc/sysconfig/network-scripts/ifcfg-ens33
			在上面进行网络设置时加入：
				DNS1=1.1.1.1
				DNS2=8.8.8.8
			这样 /etc/resolv.conf 文件就会自动用ens33中添加的DNS服务器
			
			另外一种解决办法是在NetworkManager.conf中加入dns=none,没试过。		
		
	重启网络服务：
		/etc/init.d/network restart
		service network restart
		service network-manager restart
		systemctl restart network-manager
		systemctl restart network
	
	Centos 7+防火墙：
		firewall-cmd --list-ports 查看开放的端口
		firewall-cmd --zone=public --add-port=80/tcp --permanent 开放端口
		firewall-cmd --reload 防火墙更改记得重启防火墙
		systemctl stop firewalld.service 停止防火墙，开启就是start
		firewall-cmd --zone=public --remove-port=80/tcp --permanent 关闭端口
		firewall-cmd --state 查看防火墙状态
	
	更改默认的SSH端口号：
		vi /etc/ssh/sshd_config
		#Port 22 这行可删除
		Port 1984
		/etc/init.d/sshd restart 重启SSH服务，或者 service sshd restart
		需不需要开放防火墙相应的端口呢？？？
		
	who 查看用户
	
	Centos 安装pip：
		sudo yum -y install epel-release
		sudo yum -y install python-pip
		pip install --upgrade pip
		pip -v
	
	Centos安装wget：
		yum -y install wget
		
	Centos使用源码方式安装Python 3.7：
		wget https://www.centos.bz/2018/07/centos-7-%E5%AE%89%E8%A3%85-python-3-7/
		sudo yum groupinstall "Development tools"
		sudo yum -y install zlib zlib-devel
		sudo yum -y install libffi-devel
		wget https://www.python.org/ftp/python/3.7.3/Python-3.7.3.tar.xz
		tar xJf Python-3.7.3.tar.xz
		cd Python-3.7.3
		sudo ./configure
		sudo make
		sudo make install
	
	
	Centos 错误：Cannot find a valid baseurl for repo: base	
		https://unix.stackexchange.com/questions/22924/how-can-i-fix-cannot-find-a-valid-baseurl-for-repo-errors-on-centos
		sudo dhclient
		
	Centos 删除文件（夹）：
		 rm /home/test
		 
	更改默认Python版本：
		https://askubuntu.com/questions/320996/how-to-make-python-program-command-execute-python-3
		alias python=python3

	
	查看当前系统版本：
		uname -a


	安装缺失的东西：
		apt-get install --fix-broken

	删除多余的东西：
		apt-get autoremove

	安装deb软件：
		dpkg -i xxx


检查和安装更新：
	apt-get update && apt-get upgrade

显示设备最近的变化情况：
	dmesg

将镜像刻录到U盘：
	dd if=FILE_NAME of=DEVICE_NAME bs=1M【可选，块的大小】
		if = input file
		of = output file

分区工具：
	parted
		print devices
		select /dev/sdb
		print
		mkpart primary 3063 7063 【起始位置】
		quit

ls -l
ls -a 可查看隐藏文件
ls -lh --sort=size 按大小排序
ls -lh --sort=time 时间
cd 进入～目录，用户主目录
cd . 当强目录
cd .. 当前目录的上一个目录

cat /var/log/messages
more /var/log/messages
less /var/log/messages
tail /var/log/messages 默认最后十行
tail -20 /var/log/messages 最后二十行
watch -n 2 tail /var/log/messages 每隔2秒刷新一次

文件删除、复制
	rm xx.txt
	cp x.txt x1.txt
目录删除、复制
	cp -r old new
	rm -r old

top 当前系统信息
	k 输入进程号杀掉进程 q退出

ps 显示进程信息
ps -ef 详细的

grep ssh /etc/passwd 筛选输出信息

ifconfig eth0 down/up
macchanger -m 00:00:00:00:00:10 eth0

netstat
netstat -pantu
netstat -pantu | egrep -v '0.0.0.0|:::|127.0.0.1' | awk '{print $5}' | egrep -v 'and|Address' | cut -d ':' -f 1 | sort | uniq > ip.txt

find / -name nmap 从根目录开始查找
find / -iname nmap 区分大小写

whereis nmap

vi 使用：
	dd 删除当前行
	o 当前行下一行插入一行
	y 复制
	t 粘贴

seq 9

查看各个网卡的网关：
	netstat -nr

添加默认网关：
	route add default gw 192.168.1.1

编辑DNS服务器：
	vi /etc/resolv.conf

编辑网卡设置：
	vi /etc/network/interfaces

ip addr show
ip addr show wlan0

df -ah 查看磁盘使用情况 

systemctl status udev
或 service udev status

du -sh FILE_OR_DIR 查看文件或目录大小

netstat -tulpn
	
man COMMAND 命令使用说明

/etc/init.d/ 各种启动脚本所在目录

Linux笔记本挂起后导致触摸板没反应失灵:
	禁用触摸板： rmmod psmouse
	启用触摸板： modprobe psmouse

别名：
	查看别名：alias
	删除别名：unalias

curl:
	curl www.google.com
	curl -v www.google.com 可以查看响应头
	
时间设置：
	v2ray中时间不对也无法正常翻墙
	date -R
	date --set="1989-06-04 05:00:00"
